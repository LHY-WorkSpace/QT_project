1.信号：
    自定义信号放在类声明的 singal 下
    自定义信号只有声明
    void 类型，可有参数，可重载
    触发方式：emit obj->自定义信号();


2.槽：
    自定义槽函数可放在类声明的任意位置
    void 类型，可有参数，可重载
    必须有定义

































































